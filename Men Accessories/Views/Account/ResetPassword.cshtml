@model ResetPasswordViewModel
@{
    ViewData["Title"] = "Reset Password";
    Layout = "~/Views/Shared/_ShopLayout.cshtml";
}

@section Styles {
    <partial name="_AccountAuthStyles" />
}

<div class="auth-page">

    <!-- BREADCRUMB -->
    <div class="breadcrumb">
        <a asp-controller="Home" asp-action="Index">Home</a>
        <span class="sep">/</span>
        <span class="current">Reset Password</span>
    </div>

    <!-- AUTH CARD -->
    <div class="auth-card">
        <div class="icon-circle purple">
            <svg viewBox="0 0 24 24" fill="none" stroke="#4F46E5" stroke-width="1.8">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
            </svg>
        </div>
        <div class="card-eyebrow">Account Recovery</div>
        <div class="card-title">Reset Password</div>
        <p class="card-sub">Enter your new password below to regain access to your account.</p>

        <form asp-action="ResetPassword" method="post">
            <div asp-validation-summary="All" class="validation-summary-errors"></div>

            <!-- New Password -->
            <div class="form-group">
                <label class="form-label">New Password</label>
                <input asp-for="Password" id="pass1" class="form-control" type="password" placeholder="Enter new password" />
                <span class="eye-toggle" onclick="togglePass('pass1', this)">SHOW</span>
                <span asp-validation-for="Password" class="field-validation-error"></span>
            </div>

            <!-- Confirm Password -->
            <div class="form-group">
                <label class="form-label">Confirm Password</label>
                <input asp-for="ConfirmPassword" id="pass2" class="form-control" type="password" placeholder="Confirm password" />
                <span class="eye-toggle" onclick="togglePass('pass2', this)">SHOW</span>
                <span asp-validation-for="ConfirmPassword" class="field-validation-error"></span>
            </div>

            <!-- Submit -->
            <button type="submit" class="btn-primary">Change Password</button>

            <!-- Cancel -->
            <a asp-action="Login" class="btn-ghost" style="margin-top:12px;">Cancel</a>
        </form>
    </div>

</div>

@section Scripts {
    <script>
        function togglePass(id, el) {
            var inp = document.getElementById(id);
            if (inp.type === 'password') { inp.type = 'text'; el.textContent = 'HIDE'; }
            else { inp.type = 'password'; el.textContent = 'SHOW'; }
        }
    </script>
}
